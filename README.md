# Meechu Birthday Website

To preview from files, open `dist/index.html` after running `npm run build`.

For development: run `npm run dev` and open the localhost URL printed in the terminal.

Add personal images under `public/images/` and connect them in `src/assets.ts`.
